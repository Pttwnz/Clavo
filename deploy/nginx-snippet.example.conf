# Ejemplo Nginx: clavo = Next (37891) · app = Gastro (37892)
# Copia a sites-available y ajusta rutas de certificados (certbot).
#
# upstream clavo_next { server 127.0.0.1:37891; }
# upstream clavo_gastro { server 127.0.0.1:37892; }
#
# server {
#   listen 443 ssl http2;
#   server_name clavo.gastromanager.es;
#   ssl_certificate     /etc/letsencrypt/live/clavo.gastromanager.es/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/clavo.gastromanager.es/privkey.pem;
#   location / {
#     proxy_pass http://clavo_next;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
#
# server {
#   listen 443 ssl http2;
#   server_name app.gastromanager.es;
#   ssl_certificate     /etc/letsencrypt/live/app.gastromanager.es/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/app.gastromanager.es/privkey.pem;
#   location / {
#     proxy_pass http://clavo_gastro;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
#
# Tras activar HTTPS:
# - deploy/.env → NEXT_PUBLIC_GASTRO_BASE_URL=https://app.gastromanager.es
#                 AUTH_URL=https://clavo.gastromanager.es
# - docker compose build web && docker compose up -d
# - Gastro: FLASK_SECURE_COOKIES=1 (en .env del compose) y en panel «URL pública base» = https://app.gastromanager.es
